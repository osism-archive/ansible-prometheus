---
##########################
# packages

required_packages_prometheus:
  - docker-compose
  - python-docker

##########################
# docker

docker_registry: index.docker.io

##########################
# operator user

operator_user: dragon
operator_group: "{{ operator_user }}"

##########################
# prometheus

prometheus_configuration_directory: /opt/prometheus/configuration

prometheus_host: "{{ ansible_default_ipv4.address }}"
prometheus_port: 9090

prometheus_tag: v1.8.1
prometheus_image: "{{ docker_registry }}/prom/prometheus:{{ prometheus_tag }}"

prometheus_configure_grafana_ds: false
prometheus_grafana_host: "{{ ansible_default_ipv4.address }}"
prometheus_grafana_port: 3000
prometheus_grafana_username: admin
prometheus_grafana_password: password
